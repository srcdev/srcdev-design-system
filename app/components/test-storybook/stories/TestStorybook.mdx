import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks"
import * as StoryBookComponent from "./TestStorybook.stories"

<Meta of={StoryBookComponent} />

# TestStorybook component

- A sample `*.stories.ts` file to verify:
  - Stories CSF working correctly
  - Stories MDX working correctly
  - Controls via `argTypes` working correctly

## Update layout options using Controls

### UI Controls available:

- layoutVariant
- layoutTag
  <Canvas of={StoryBookComponent.Default} />
